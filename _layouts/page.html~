---
layout: default
---
<article class="post">

  <header class="post-header">
    <h1 class="post-title">{{ page.title | escape }}</h1>
  </header>

  <div class="post-content">
    {{ content }}
  </div>
{% if page.title == "Hakkımızda"%}
 <div class="members">
 <h2>Ekibimiz</h2>
{% for mem in site.data.members %}
		<div class="pmcolumn">
			<div class="per-column">
				{% if mem.image %}
				<div class="per-image"><img src="{{site.url}}{{ mem.image }}" width="200" height="250"></div>
				{% else %}
				<div class="per-image"><img src="{{site.url}}/assets/img/members/null.jpg" width="200" height="250"></div>
				{% endif %}
					<div class="per-content">
							{% if mem.social %}
								<h3><a href="{{ mem.social }}" > {{ mem.name }}</a></h3>
							{% else %}
								<h3>{{ mem.name }}</h3>
							{% endif %}
							<h4>{{ mem.task }}</h4>
					</div>				
			</div>
		</div>
{% endfor %}
</div>
{% endif %}

</article>
